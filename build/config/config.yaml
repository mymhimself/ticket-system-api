
database:
  postgres:
    username: "postgre"
    password: "12345"
    db_name: "ticket"
    host: "localhost"
    port: "5432"
    ssl_mode: "disable"
    time_zone: "Asia/Tehran"
    charset: "utf8mb4"
    migration: false

server:
  port: 8080

cabin:
  path: "./build/casbin/"

logger:
  max_age: 720h
  max_size: 20MB
  filename_pattern: "%Y-%m-%dT%H:%M"
  rotation_time: "24h"
  internal_path: "./logs/internal"


sms:
  kavenegar:
    token: "token"

validation:
  username_min_length: 3
  username_max_length: 50
  password_min_letters: 3

auth:
  token_expiration_in_minute: 15
  refresh_expiration_in_minute: 525600
  jwt_secret: "jwt@secret"
